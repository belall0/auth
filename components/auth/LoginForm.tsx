import FormWrapper from './FormWrapper';

const LoginForm = () => {
  return (
    <FormWrapper
      formTitle="Login"
      formType="login"
      formDescription="Login to your account">
      <p>This is the form elements</p>
    </FormWrapper>
  );
};

export default LoginForm;
